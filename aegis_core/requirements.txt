# Aegis — AI-Powered Code Security Platform
# Install with: pip install -r requirements.txt

# LLM gateway — supports OpenAI, Gemini, Anthropic, and 100+ other models
litellm>=1.40.0

# Structured output validation
pydantic>=2.0.0

# Web server for GitHub webhook integration
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Async HTTP (used internally by FastAPI)
httpx>=0.27.0

# Phase 1: Hybrid Discovery & CI/CD
semgrep>=1.60.0
PyGithub>=2.0.0
python-dotenv>=1.0.0
pyyaml>=1.0.0
cryptography>=41.0.0
PyJWT>=2.8.0
tenacity>=8.2.0
tiktoken>=0.6.0
